side_length = 4 # You can change this number for different sizes

# Outer loop for rows
for i in range(side_length):
    # Inner loop for columns, prints stars in each row
    print("*" * side_length)